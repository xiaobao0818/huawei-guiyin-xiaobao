# 华为鲸鸿动能通用自归因平台 - 技术方案

> 信息基于 developer.huawei.com 官方文档、神策数据(Sensors Analytics)鲸鸿动能接入文档、华为快游戏外部买量指南、
> 转化跟踪接口对接说明(中国大陆)v2.x 等独立来源整理。
> 未参考用户已有本地文档。

---

## 一、背景

### 1.1 场景
- IAA 游戏，在华为应用市场/Games 上架
- 三个包体：**RPK（快游戏）、APK（Android）、HAP（鸿蒙）**
- 在鲸鸿动能（Petal Ads）投放买量广告
- 需要自归因：自己搭建服务端接收点击回调、匹配用户转化、回传鲸鸿动能

### 1.2 目标
构建一个**引擎无关、多游戏、多包体**的通用自归因平台，满足：
1. 任何游戏引擎（Unity/Cocos/Unreal/自研）都能接入
2. 支持多个游戏同时使用
3. 完整覆盖 RPK / APK / HAP 三种包体
4. 归因事件和参数可自由配置
5. 管理后台可运营

---

## 二、鲸鸿动能归因体系（独立研究整理）

### 2.1 归因链路

```
用户点击广告
    │
    ▼
鲸鸿动能将宏参数展开，GET 请求广告主监测链接
    │ 携带: callback, oaid, campaign_id, adgroup_id, content_id, trace_time, ip, ...
    ▼
广告主服务端存储：oaid → callback 映射（Redis）
    │
用户打开游戏 → 客户端获取 OAID → POST 到服务端上报激活
    │
    ▼
服务端匹配 oaid → 找到 callback → 构造 JSON → 签名 → POST 鲸鸿动能回传接口
    │
    ▼
鲸鸿动能收到回传，完成归因，用于 oCPX 优化
```

### 2.2 监测链接宏参数（已验证）

在鲸鸿动能后台配置监测链接时，使用以下宏参数：

| 宏参数 | 说明 | 必选 |
|--------|------|------|
| `__CALLBACK__` | 回传标识，必须原样回传 | ✅ |
| `__OAID__` | 设备匿名标识(明文) | ✅ |
| `__AID__` / `__ADGROUP_ID__` | 广告组/任务 ID | ✅ |
| `__CID__` / `__CAMPAIGN_ID__` | 广告计划 ID | ✅ |
| `__CONTENT_ID__` | 创意/素材 ID | 建议 |
| `__TS__` | 点击时间戳(毫秒) | 建议 |
| `__IP__` | 用户 IP | 建议 |
| `__UA__` / `__USER_AGENT__` | 用户代理 | 建议 |
| `__CORP_ID__` | 广告主账户 ID | 建议 |
| `__APP_ID__` | 投放应用 ID | 可选 |
| `__ACTION_TYPE__` | 归因类型(IMP/CLICK/DEEPLINKCLICK) | 可选 |
| `__CHANNEL__` | 媒体渠道 | 可选 |
| `__TRACKING_ENABLED__` | 跟踪许可(0/1) | 可选 |

> 注意：OAID 和 OAID_MD5 不能同时配置。选择 OAID_MD5 时宏参数名也要写成 `__OAID__`，否则影响归因。

### 2.3 监测链接完整示例

```
https://your-domain.com/api/v1/click?callback=__CALLBACK__&oaid=__OAID__&campaign_id=__CID__&adgroup_id=__AID__&content_id=__CONTENT_ID__&ts=__TS__&ip=__IP__&ua=__UA__&action_type=__ACTION_TYPE__&tracking_enabled=__TRACKING_ENABLED__
```

### 2.4 转化回传 API

#### 回传请求体（已验证）

```json
{
  "callback": "鲸鸿动能传来的原始值(URL编码)",
  "content_id": "创意ID",
  "campaign_id": "计划ID",
  "oaid": "设备OAID(明文)",
  "tracking_enabled": "1",
  "conversion_type": "activate",
  "conversion_time": "1716900000",
  "timestamp": "1716900000000"
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `callback` | string | ✅ | 鲸鸿动能传来的 callback，URL 编码的原文 |
| `content_id` | string | 否 | 创意 ID |
| `campaign_id` | string | 否 | 计划 ID |
| `oaid` | string | 否 | 设备 OAID，无则传空串 |
| `tracking_enabled` | string | 否 | 0=不允许跟踪, 1=允许跟踪 |
| `conversion_type` | string | ✅ | 转化事件类型 |
| `conversion_time` | string | ✅ | 转化发生时间，**单位秒** |
| `timestamp` | string | ✅ | 请求发起时间，**单位毫秒** |

#### conversion_type 可取值（已验证）

| 值 | 说明 | oCPX 可用 |
|----|------|-----------|
| `activate` | 激活 | ✅ |
| `register` | 注册 | ✅ |
| `retain` | 留存 | ✅ |
| `paid` | 付费 | ✅ |
| `browse` | 浏览 | - |
| `collection` | 收藏 | - |
| `addToCart` | 加入购物车 | - |
| `preOrder` | 预购 | - |
| `custom` | 自定义 | 视情况 |
| `form_submit` | 表单提交 | - |
| `consult` | 咨询 | - |
| `custom_acquisit` | 自定义获客 | - |
| `book` | 预约 | - |
| `custom_landingpage` | 自定义落地页 | - |

#### 付费扩展字段

当 `conversion_type=paid` 时，可附加：

```json
{
  "conversion_extend": {
    "revenue": "6.00",
    "currency": "CNY"
  }
}
```

**currency 支持**: CNY, USD, EUR, JPY, GBP

#### 密钥与鉴权

- **密钥级别**: 账户级（同一广告主账户下所有资产共用）
- **密钥格式**: Base64 编码字符串
- **获取方式**: 鲸鸿动能后台 → 工具 → 事件资产管理 → 页面右上角「小眼睛」按钮
- **鉴权方式**: HMAC-SHA256 签名，请求头格式为 `Authorization: Digest validTime="时间戳", response="HMAC-SHA256签名(Base64密钥作为密钥)"`

> ⚠️ 注意：签名用的密钥是 Base64 字符串的**原始字节**，不是解码后的内容。
> 服务器时间需 NTP 同步，偏差超过 5 分钟会导致鉴权失败。

### 2.5 归因窗口期

| 类型 | 窗口期 | 说明 |
|------|--------|------|
| 点击归因 | **30 天** | 点击后 30 天内激活/转化有效 |
| 曝光归因 | **2 天** | 曝光后 2 天内转化有效 |
| 激活后 | **180 天** | 激活后的深度事件回传 |

### 2.6 资产类型对比

| 资产类型 | 适用包体 | 归因方式 |
|----------|---------|---------|
| 应用资产 (Native App) | APK | 自有分析工具 / HA / 第三方 |
| 鸿蒙应用资产 | HAP | 自有分析工具 / HA / 第三方 / 应用归因服务 |
| 快应用资产 (Quick App) | RPK | 自有分析工具 / HA |
| 网页资产 (Web Page) | Web/H5 | Pixel / 自有分析工具 |

> 三种包体需要在鲸鸿动能后台**分别创建**三个独立的资产，每个填入对应的监测链接。

---

## 三、各平台设备 ID 获取

### 3.1 APK（Android）

```java
// 集成 com.huawei.hms:ads-identifier
import com.huawei.hms.ads.identifier.AdvertisingIdClient;

// 获取 OAID（需要在子线程中调用）
InfoResult result = AdvertisingIdClient.getAdvertisingIdInfo(context);
String oaid = result.getId();
```

**权限要求**: 无需额外权限，但 OAID 受用户「限制广告跟踪」开关影响（关闭时返回全 0）。

**Install Referrer（增强归因）**:
```java
// 集成 com.huawei.hms:ads-installreferrer
// 可以获取应用安装来源信息
```

### 3.2 HAP（鸿蒙）

```typescript
// 集成 @kit.AdsKit
import { identifier } from '@kit.AdsKit';

const oaid = await identifier.getOAID();
```

**权限要求**: `ohos.permission.APP_TRACKING_CONSENT`（鸿蒙 5.0+），需要在运行时请求用户授权。

### 3.3 RPK（快游戏）⚠️

快游戏的 OAID 获取有特殊限制：

**方案 A（推荐）: `qg.getOAID()`**
```javascript
// 华为快游戏 SDK 提供
qg.getOAID({
  success: (res) => { /* res.oaid */ },
  fail: (err) => { /* 获取失败 */ }
})
```
- **前提**: 必须在华为官方快应用加载器中运行
- 需要在 `manifest.json` 中配置 `features: [{ "name": "system.device" }]`

**方案 B（备选）: `qg.getLaunchOptionsSync()`**
```javascript
// 获取 URL 参数，其中包含归因参数
const opts = qg.getLaunchOptionsSync();
// opts.query: { adid, creativeid, creativetype, clickid, ... }
```
- 不依赖 OAID，通过 URL 参数中的 click ID 传递归因信息
- 适用于 Deeplink 投放场景

**方案 C（降低要求）: `qa.getId()`**
```javascript
qa.getId({
  type: ['device'],
  success: (data) => { /* data.device = AAID */ }
})
```
- 返回的是 AAID（应用级标识），不是 OAID
- 不同 App 在同一设备上 AAID 不同，无法跨 App 归因
- 仅作为兜底方案，用于内部数据统计

**RPK 归因策略**: 
1. 优先使用 `qg.getOAID()` 获取 OAID
2. 如果失败，使用 `qg.getLaunchOptionsSync()` 获取 clickid/adid
3. 最坏情况使用 `qa.getId()` + 服务端 IP/UA 指纹匹配

---

## 四、系统架构

### 4.1 总体架构图

```
┌──────────────────────────────────────────────────┐
│                  鲸鸿动能广告平台                   │
│  展开宏 → GET 监测链接 → 接收转化回传             │
└───────┬──────────────────────────▲───────────────┘
        │ callback+oaid            │ 签名回传
        ▼                          │
┌──────────────────────────────────────────────────┐
│              自归因平台（Spring Boot）              │
│                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │Click收集  │ │ 归因匹配  │ │ 回传网关(签名)    │ │
│  │/api/click│ │ 引擎     │ │ 重试/降级         │ │
│  └────┬─────┘ └────┬─────┘ └────────┬─────────┘ │
│       │             │                │           │
│  ┌────▼─────────────▼────────────────▼─────────┐ │
│  │              Redis + MySQL                   │ │
│  │  Redis: click_cache(oaid→callback, TTL 7天)  │ │
│  │  MySQL: 归因记录/事件配置/回传日志           │ │
│  └──────────────────────────────────────────────┘ │
│                                                  │
│  ┌──────────────────────────────────────────────┐ │
│  │              管理后台 (Vue3)                   │ │
│  │  游戏管理 │ 事件配置 │ 归因查询 │ 数据报表    │ │
│  └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
         ▲                        ▲
         │ POST /api/v1/report    │ POST /api/v1/report
         │                        │
┌────────┴────────┐    ┌─────────┴─────────┐
│ 游戏客户端(APK)  │    │ 游戏客户端(HAP/RPK) │
│ 任意引擎        │    │ 任意引擎           │
└─────────────────┘    └───────────────────┘
```

### 4.2 客户端接入协议（引擎无关）

**客户端只需要发一个 HTTP POST 请求，不依赖任何 SDK。**

```
POST /api/v1/report
Content-Type: application/json
X-Attribution-Api-Key: your-report-api-key

{
  "game_id":      "bead_master",
  "platform":     "apk",
  "event":        "activate",
  "device": {
    "oaid": "1fe9a970-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  },
  "event_params": {
    "revenue": 6.00,
    "currency": "CNY",
    "custom_key": "任意值"
  },
  "app": {
    "version": "1.2.3"
  },
  "ts": 1716900000000
}
```

**这就是全部。任何引擎只要能发 HTTP 请求就能接入。**

---

## 五、服务端详细设计

### 5.1 模块结构

```
attribution-platform/
├── attribution-server/
│   ├── attribution-api/                 # 对外 API
│   │   └── com/game/attribution/api/
│   │       ├── controller/
│   │       │   ├── ClickController.java        # GET  /api/v1/click
│   │       │   └── ReportController.java       # POST /api/v1/report
│   │       └── model/
│   │           ├── ClickRequest.java
│   │           └── ReportRequest.java
│   │
│   ├── attribution-core/                # 核心引擎
│   │   └── com/game/attribution/core/
│   │       ├── engine/
│   │       │   └── AttributionEngine.java      # 归因匹配主引擎
│   │       ├── matcher/
│   │       │   ├── OaidMatcher.java            # OAID 精确匹配
│   │       │   └── FingerprintMatcher.java     # 指纹降级匹配(可选)
│   │       ├── callback/
│   │       │   ├── CallbackService.java        # 构造+发送回传
│   │       │   ├── SignatureHelper.java        # HMAC-SHA256 签名
│   │       │   └── CallbackRetryService.java   # 重试机制
│   │       └── event/
│   │           └── EventRouter.java            # 事件→conversion_type 路由
│   │
│   ├── attribution-admin/               # 管理后台 API
│   │   └── com/game/attribution/admin/
│   │       ├── controller/
│   │       │   ├── GameController.java         # 游戏注册/管理
│   │       │   ├── EventConfigController.java  # 事件定义管理
│   │       │   └── DashboardController.java    # 数据查询
│   │       ├── service/
│   │       │   ├── GameService.java
│   │       │   ├── EventConfigService.java
│   │       │   └── AnalyticsService.java
│   │       └── dto/
│   │
│   └── attribution-common/              # 公共模块
│       └── com/game/attribution/common/
│           ├── entity/                         # JPA Entity
│           │   ├── GameConfig.java
│           │   ├── EventDefinition.java
│           │   ├── ClickRecord.java
│           │   ├── AttributionRecord.java
│           │   └── CallbackLog.java
│           └── enums/
│
├── admin-frontend/                      # 管理后台前端
│   ├── src/views/
│   │   ├── Dashboard.vue
│   │   ├── GameManage.vue
│   │   ├── EventConfig.vue
│   │   └── AttributionData.vue
│   └── package.json
│
└── docs/                                # 接入文档
    ├── 接入指南-通用.md
    ├── APK-OAID获取指南.md
    ├── HAP-OAID获取指南.md
    └── RPK-归因适配指南.md
```

### 5.2 数据库设计

```sql
-- 游戏配置
CREATE TABLE game_config (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    game_id         VARCHAR(64)  NOT NULL UNIQUE,
    game_name       VARCHAR(128) NOT NULL,
    platforms       VARCHAR(64)  NOT NULL DEFAULT 'apk,hap,rpk',   -- 逗号分隔
    secret_key      VARCHAR(512) NOT NULL,       -- Base64密钥(AES加密存储)
    attribution_window_days INT DEFAULT 30,
    callback_retry_max    INT DEFAULT 3,
    status          TINYINT DEFAULT 1,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 事件定义（可配置）
CREATE TABLE event_definition (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    game_id         VARCHAR(64)  NOT NULL,
    event_name      VARCHAR(64)  NOT NULL,        -- 客户端上报的事件名
    display_name    VARCHAR(128),
    conversion_type VARCHAR(32),                  -- 映射鲸鸿动能 conversion_type(NULL=不回传)
    param_schema    JSON,                          -- 参数 JSON Schema
    is_preset       TINYINT DEFAULT 0,
    enabled         TINYINT DEFAULT 1,
    UNIQUE KEY uk_game_event (game_id, event_name)
);

-- 点击记录
CREATE TABLE click_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    game_id         VARCHAR(64)  NOT NULL,
    oaid            VARCHAR(128) NOT NULL,
    callback        TEXT         NOT NULL,         -- 原始 callback
    campaign_id     VARCHAR(64),
    adgroup_id      VARCHAR(64),
    content_id      VARCHAR(64),
    click_time      BIGINT       NOT NULL,
    ip              VARCHAR(45),
    user_agent      VARCHAR(512),
    platform        VARCHAR(20),
    matched         TINYINT DEFAULT 0,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_game_oaid (game_id, oaid),
    INDEX idx_click_time (click_time)
);

-- 归因记录
CREATE TABLE attribution_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    game_id         VARCHAR(64)  NOT NULL,
    click_id        BIGINT,                        -- FK → click_record
    oaid            VARCHAR(128) NOT NULL,
    event_type      VARCHAR(64)  NOT NULL,
    event_params    JSON,
    conversion_type VARCHAR(32),
    conversion_time BIGINT,                        -- 秒
    revenue         DECIMAL(12,2) DEFAULT 0,
    currency        VARCHAR(8) DEFAULT 'CNY',
    platform        VARCHAR(20),
    app_version     VARCHAR(32),
    callback_status VARCHAR(16) DEFAULT 'pending', -- pending/success/failed
    callback_resp   TEXT,
    retry_count     INT DEFAULT 0,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_game_oaid (game_id, oaid),
    INDEX idx_game_event (game_id, event_type)
);

-- 回传日志
CREATE TABLE callback_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    attribution_id  BIGINT NOT NULL,
    game_id         VARCHAR(64) NOT NULL,
    request_url     VARCHAR(512),
    request_body    TEXT,
    response_code   INT,
    response_body   TEXT,
    duration_ms     INT,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 5.3 API 接口列表

| 端点 | 方法 | 调用方 | 说明 |
|------|------|--------|------|
| `/api/v1/click` | GET | 鲸鸿动能 | 接收宏展开的点击回调 |
| `/api/v1/report` | POST | 游戏客户端 | 上报激活/付费/自定义事件 |
| `/api/v1/health` | GET | 监控 | 健康检查 |
| `/admin/api/games` | CRUD | 管理后台 | 游戏管理 |
| `/admin/api/games/{id}/events` | CRUD | 管理后台 | 事件配置 |
| `/admin/api/attribution` | GET | 管理后台 | 归因记录查询 |
| `/admin/api/dashboard` | GET | 管理后台 | 统计面板 |

### 5.4 归因引擎核心流程

```
收到 /api/v1/report
    │
    ├─ 1. 参数校验（game_id、event、oaid 等）
    │
    ├─ 2. 查询事件配置 → 获取 conversion_type
    │     └─ conversion_type 为空 → 仅记录，不回传（内部事件）
    │
    ├─ 3. 归因匹配（按优先级）
    │     ├─ OAID 精确匹配: Redis GET attribution:click:{game_id}:{oaid}
    │     ├─ 匹配成功 → 获取 callback、campaign_id 等
    │     └─ 匹配失败 → 记录日志，返回 "no_match"
    │
    ├─ 4. 去重检查
    │     └─ activate 事件：该 oaid 是否已有成功归因(pending/success)
    │
    ├─ 5. 构造回传 JSON
    │     {
    │       callback: "...",
    │       conversion_type: "activate",
    │       conversion_time: "秒",
    │       timestamp: "毫秒",
    │       oaid: "...",
    │       tracking_enabled: "1",
    │       conversion_extend: { ... }    // 仅付费事件
    │     }
    │
    ├─ 6. HMAC-SHA256 签名
    │     - 密钥: game_config.secret_key（Base64 字符串的字节）
    │     - 算法: HMAC-SHA256(body_bytes, key_bytes)
    │     - 请求头: Authorization: Digest validTime="ts", response="hex(sig)"
    │
    ├─ 7. POST 鲸鸿动能回传地址
    │     - resultCode=0 → 成功，更新状态
    │     - resultCode=1 → 签名错误，记录告警
    │     - resultCode=2 → 参数非法，检查字段
    │     - 失败 → 重试（最多 N 次，指数退避）
    │
    └─ 8. 持久化归因记录 + 回传日志
```

### 5.5 服务端部署检查清单

| 项目 | 要求 |
|------|------|
| 协议 | **HTTPS**（华为只接受 HTTPS） |
| 域名 | `.com/.cn/.net/.top/.cc/.xyz/.vip/.huawei` |
| 时间同步 | NTP 同步，偏差 < 5 分钟 |
| Redis TTL | 点击缓存 7 天（归因窗口内需求） |
| 密钥存储 | AES 加密存储，不落明文 |

---

## 六、管理后台设计

### 6.1 页面功能

| 页面 | 功能 |
|------|------|
| **概览面板** | 总 DAU/新增/付费/ROI 卡片、归因渠道分布饼图、今日回传成功率 |
| **游戏管理** | 新增/编辑游戏，配置 game_id、密钥、归因窗口、支持平台 |
| **事件配置** | 定义事件类型、设置参数 Schema、映射 conversion_type、启用/禁用 |
| **归因查询** | 按 oaid/时间/事件类型查询归因记录，导出 CSV |
| **回传日志** | 查看每次回传的请求/响应详情，排查签名/参数问题 |

### 6.2 事件配置示例

在管理后台为一个游戏配置事件：

```
游戏: bead_master

事件列表:
┌────────────────┬──────────┬─────────────────────┬────────┐
│ event_name     │ 显示名称  │ conversion_type     │ 启用   │
├────────────────┼──────────┼─────────────────────┼────────┤
│ activate       │ 激活     │ activate            │ ✅     │
│ register       │ 注册     │ register            │ ✅     │
│ purchase       │ 付费     │ paid                │ ✅     │
│ retain_1d      │ 次留     │ retain              │ ✅     │
│ level_complete │ 通关     │ (不回传,仅内部分析)  │ ✅     │
│ tutorial_done  │ 教程完成  │ custom              │ ✅     │
└────────────────┴──────────┴─────────────────────┴────────┘

purchase 事件参数 Schema:
  - revenue: number, 必填, "支付金额(元)"
  - currency: string, 默认"CNY", "货币代码"
  - product_id: string, "商品ID"
  - order_id: string, "订单号"
  - is_first_pay: boolean, "是否首充"
```

---

## 七、接入流程

### 7.1 服务端部署

```
1. 部署归因服务端（Java Spring Boot + Redis + MySQL）
2. 配置 HTTPS 域名
3. 配置 NTP 时间同步
```

### 7.2 鲸鸿动能后台配置

```
1. 登录 ads.huawei.com
2. 工具 → 事件资产管理 → 新建资产
   - RPK 包体 → 选择「快应用资产」
   - APK 包体 → 选择「应用资产」
   - HAP 包体 → 选择「鸿蒙应用资产」
3. 分析工具 → 选择「自有分析工具」
4. 填写监测链接:
   https://your-domain.com/api/v1/click?game_id=xxx&callback=__CALLBACK__&oaid=__OAID__&campaign_id=__CID__&adgroup_id=__AID__&content_id=__CONTENT_ID__&ts=__TS__&ip=__IP__&ua=__UA__&platform=__PLATFORM__
5. 复制密钥（页面右上角小眼睛），填入管理后台的游戏配置
6. 创建转化事件 → 联调测试
```

### 7.3 游戏客户端接入

```
1. 在管理后台注册游戏 → 获取 game_id
2. 按各平台指引获取 OAID（见第三章节）
3. 在以下时机调用 POST /api/v1/report:
   - 游戏首次启动 → event: "activate"
   - 用户注册 → event: "register"
   - 付费成功 → event: "purchase", event_params: { revenue: 6.00 }
   - 其他自定义事件
```

---

## 八、验证方案

### 8.1 服务端自测

```bash
# 1. 模拟鲸鸿动能点击
curl "https://your-domain.com/api/v1/click?game_id=test_game&callback=CALLBACK_abc123&oaid=OAID_test_001&campaign_id=camp_1&adgroup_id=adg_1&ts=1716900000000&ip=1.2.3.4"

# 2. 检查 Redis
redis-cli GET "attribution:click:test_game:OAID_test_001"

# 3. 模拟激活上报
curl -X POST https://your-domain.com/api/v1/report \
  -H "Content-Type: application/json" \
  -H "X-Attribution-Api-Key: your-report-api-key" \
  -d '{
    "game_id": "test_game",
    "platform": "apk",
    "event": "activate",
    "device": {"oaid": "OAID_test_001"},
    "ts": 1716900100000
  }'

# 4. 检查回传日志
curl https://your-domain.com/admin/api/callback-log?game_id=test_game
```

### 8.2 鲸鸿动能联调

```
1. 准备一台华为手机，获取其 OAID
   （设置 → 隐私 → 广告与隐私 → 查看 OAID）
2. 在鲸鸿动能后台 → 事件概览 → 选择事件 → 点击「联调」
3. 输入测试 OAID，模拟点击
4. 手机下载/打开游戏 → 触发激活上报
5. 查看鲸鸿动能后台联调状态 → 「启用」= 成功
6. 需要在神策/内部查询到渠道匹配成功的激活事件
```

### 8.3 付费联调

```
1. 在游戏内完成真实付费
2. 检查服务端回传日志，确认 conversion_type=paid 的请求
3. 检查华为返回 resultCode=0
4. 5-10 分钟后查看鲸鸿动能转化报表
```

---

## 九、实施优先级

| 优先级 | 模块 | 产出 |
|--------|------|------|
| **P0** | 归因核心：Click 接收 + Report 接收 + 匹配 + 回传 | 最小可用归因 |
| **P0** | game_config 表 + 密钥管理 | 多游戏隔离 |
| **P0** | APK OAID 获取指引文档 | 第一个包体可接入 |
| **P1** | HAP OAID 获取指引文档 | 鸿蒙包体可接入 |
| **P1** | RPK OAID/clickid 获取指引文档 | 快游戏可接入 |
| **P1** | 事件配置系统 | 可自定义事件和参数 |
| **P1** | 管理后台基础页面 | 运营自助配置 |
| **P2** | 指纹降级匹配（RPK 兜底） | RPK 可靠性增强 |
| **P2** | 管理后台报表 | 数据可视化 |
| **P3** | 多渠道扩展（海外 GAID/IDFA） | 海外投放 |

---

## 十、技术风险与应对

| 风险 | 应对 |
|------|------|
| RPK 端 OAID 获取失败 | 降级方案: `qg.getLaunchOptionsSync()` 获取 clickid/adid 参数 |
| 用户关闭广告跟踪 | OAID 返回全 0，该用户放弃归因，不影响主流程 |
| 签名鉴权失败 (resultCode=1) | 检查密钥是否为 Base64 格式；检查服务器时间 NTP 同步；确认签名用密钥字节而非解码后 |
| 参数非法 (resultCode=2) | 检查 conversion_time 是秒不是毫秒；确认必填字段完整 |
| 密钥泄露 | 数据库 AES 加密存储；管理后台脱敏显示 |

---

## 附录：资料引用

- [鲸鸿动能事件资产管理概述（中国大陆）](https://developer.huawei.com/consumer/cn/doc/promotion/ads_gongju08-0000001469108293)
- [自有分析工具接入（非中国大陆）](https://developer.huawei.com/consumer/cn/doc/promotion/tracking-zi-0000001092895300)
- [监测链接跟踪-鸿蒙应用资产](https://developer.huawei.com/consumer/cn/doc/promotion/ads-hmysjclj-0000002390414717)
- [分析平台接入及 Referrer 归因](https://developer.huawei.com/consumer/cn/doc/promotion/tracking-fenxipintai-0000001262289582)
- [华为快游戏外部买量指南](https://developer.huawei.com/consumer/cn/doc/quickapp-Guides/quickgame-purchase-0000001828069481)
- [神策数据-华为鲸鸿动能转化跟踪文档](http://manual.sensorsdata.cn/sensorsadstracking/latest/%E5%8D%8E%E4%B8%BA-%E9%B2%B8%E9%B8%BF%E5%8A%A8%E8%83%BD-158434904.html)
- 鲸鸿动能转化跟踪接口对接说明(中国大陆) v2.x
