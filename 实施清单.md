# 实施任务清单

> 最后更新: 2026-05-29

---

## P0 - 核心归因（最小可用版本） ✅ 已完成

### 后端
- [x] 创建 Spring Boot 项目骨架（attribution-server）
- [x] 实现 `ClickController` — GET `/api/v1/click`，接收鲸鸿动能点击回调
- [x] 实现 `ReportController` — POST `/api/v1/report`，接收客户端事件上报
- [x] 实现 Redis 点击缓存服务 — key: `attribution:click:{game_id}:{oaid}`, TTL 7天
- [x] 实现 `AttributionEngine` — OAID/GAID/IDFA/指纹匹配 + 归因窗口校验 + 事件幂等去重
- [x] 实现 `SignatureUtil` — HMAC-SHA256 签名
- [x] 实现 `CallbackService` — 构造回传 JSON + POST 鲸鸿动能
- [x] 实现 `CallbackRetryService` — 持久化任务 + 指数退避 + 分布式锁
- [x] 数据库建表 — game_config / click_record / attribution_record / callback_log / callback_task / event_task (Flyway V1-V4)
- [x] ClickController IP 速率限制 (Redis 滑动窗口 + Lua 原子操作)
- [x] callback URL 自动解码 (URLDecoder + fail-safe)
- [x] HTTPS 支持 (Nginx SSL 终端)

### 管理后台
- [x] 游戏管理页面 — 新增/编辑游戏，配置 game_id + 密钥 + 归因窗口
- [x] 归因查询页面 — 多维筛选 + 分页 + pageSize 切换
- [x] 前端登录页 (HTTP Basic Auth, sessionStorage)

### 文档
- [x] APK OAID 获取指引
- [x] 客户端接入指南

### 联调验证
- [x] 服务端自测（curl 模拟 click + report）
- [ ] 鲸鸿动能后台配置监测资产 + 联调
- [ ] 真实手机端到端测试

---

## P1 - 多平台完善 ✅ 已完成

### 后端
- [x] 实现事件配置系统 — 事件定义 CRUD + 参数/回传规则 JSON 合法性校验
- [x] 实现 `EventRouter` — 事件名 → conversion_type 映射 (TTL 30min + max 10000 缓存)
- [x] 实现付费事件扩展字段支持（conversion_extend）
- [x] 实现回传规则引擎 (CallbackRuleEvaluator: threshold/time_window/and/or)
- [x] 实现异步事件 Worker (EventWorker: event_task 表 + 可配置线程池)
- [x] 实现留存检查任务 (RetentionCheckTask, 基于 retain_days 自动生成到期留存记录并入队回传)
- [x] 异步上报支持 (POST /api/v1/report?async=true → EventTask 入队)

### 管理后台
- [x] 事件配置页面 — 新增/编辑事件类型、配置参数、映射 conversion_type
- [x] 回传日志查询（后端接口 + 归因查询页日志抽屉）
- [x] Dashboard 数据看板 (6 指标卡片 + Redis 5min 缓存)

### 文档
- [x] HAP OAID 获取指引
- [x] RPK 归因适配指引

### 联调验证
- [ ] HAP 端到端联调
- [ ] RPK 端到端联调

---

## P2 - 增强功能 ✅ 已完成

### 后端
- [x] 指纹降级匹配（FingerprintMatcher）— IP 前缀 DB 过滤 + 加权评分
- [x] 多渠道设备 ID 匹配（OAID → GAID → IDFA → 指纹四级降级）
- [x] 回传任务异步化（CallbackTask 持久化 + 定时 Worker + 分布式锁）
- [x] 再归因回传（保护期 + 沉默期判断 + window_config 配置）
- [x] click_record 乐观锁 (@Version 防并发重复匹配)
- [x] 回传队列积压 Prometheus 监控 (attribution.callback.queue_depth gauge)
- [x] 批量数据清理 (LIMIT 1000 分批 DELETE)

### 管理后台
- [x] 概览面板 — 今日点击/激活/付费/收入/回传成功率/游戏总数
- [ ] 转化漏斗报表
- [ ] 数据导出 CSV

### 可观测性
- [x] Prometheus 指标导出 (AttributionMetrics: events/match/callback/retries/clicks/latency/funnel/queue_depth)
- [x] Grafana 看板 JSON (grafana-dashboard.json)
- [x] Actuator 健康检查 (/actuator/health, /api/v1/health + DB/Redis 连通性检查)

### 安全加固
- [x] AES-256-GCM 密钥加密存储
- [x] ReportApiKeyFilter 常量时间比较 (MessageDigest.isEqual)
- [x] DebugApiKeyFilter 常量时间比较
- [x] 管理后台 Basic Auth + BCrypt
- [x] Nginx 安全头 (CSP/X-Frame-Options/X-Content-Type-Options/XSS/Referrer)
- [x] 回传日志 body 截断 (500 字符)

---

## P3 - 运营优化

### 后端
- [ ] 多语言支持（海外市场扩展）
- [ ] SDK 化封装（可选，降低接入门槛）
- [ ] 留存回传统计上报（RetentionCheckTask 待完善）
- [ ] 转化漏斗报表

### 文档
- [ ] 接入文档自动生成（从事件配置导出）

---

## 代码审查修复记录

| 日期 | 提交 | 修复内容 |
|------|------|------|
| 2026-05-26 | `0065171` | 4 严重 + 6 高危 + 5 中低 (首次审查) |
| 2026-05-26 | `bbb2989` | 9 个安全加固 + 代码质量 |
| 2026-05-28 | `ddd88cc` | 全面架构升级 (对标引力引擎/SolarEngine) |
| 2026-05-28 | `e73ae98` | 8 文件修复 (C1-C4/H1-H3 等) |
| 2026-05-28 | `ad7c6ed` | 17 文件修复 (3 严重 + 5 高危 + 6 中低) |
| 2026-05-28 | `d43a9d9` | 文档: 实施清单 + 审查报告更新 |
| 2026-05-29 | `b9c00a4` | 功能完整性优化: GAID/IDFA + 异步处理 + 再归因 + 乐观锁 + 队列监控 |
| 2026-05-29 | `932407d` | 修复 AttributionEngine 缺少 LocalDateTime import |

---

## 功能完整性验证 (2026-05-29)

### 编译/类型检查

| 检查项 | 结果 |
|--------|:--:|
| 前端 TypeScript (`vue-tsc --noEmit`) | ✅ 0 错误 |
| 前端 npm 依赖 | ✅ 0 漏洞 |
| Java import 完整性 | ✅ 全部可解析 |
| `@EnableScheduling` | ✅ 已配置 |

### 前后端 API 对账 (16/16)

| 前端 | 后端 | 状态 |
|------|------|:--:|
| `GET /admin/api/dashboard` | `DashboardController` | ✅ |
| `GET/POST/PUT/DELETE /admin/api/games/**` | `GameController` | ✅ |
| `GET/POST/PUT/DELETE /admin/api/events/**` | `EventConfigController` | ✅ |
| `GET /admin/api/games/{id}/events` | `EventConfigController` | ✅ |
| `GET /admin/api/attribution**` | `DashboardController` | ✅ |
| `GET /admin/api/stats/{id}` | `DashboardController` | ✅ |
| `GET /admin/api/callback-logs**` | `DashboardController` | ✅ |
| `GET /api/v1/click` | `ClickController` | ✅ |
| `POST /api/v1/report` | `ReportController` | ✅ |
| `GET /api/v1/health**` | `HealthController` | ✅ |

### 定时任务 (6/6 已调度)

| 组件 | 频率 | 状态 |
|------|------|:--:|
| `CallbackRetryService` | 10s | ✅ |
| `EventWorker` (消费) | 1s | ✅ |
| `EventWorker` (stale回收) | 5min | ✅ |
| `EventWorker` (清理) | 每日 4:00 | ✅ |
| `DataCleanupTask` | 每日 3:00 | ✅ |
| `RetentionCheckTask` | 每日 2:00 | ✅ |
| `CallbackLogWriter` | 5s | ✅ |
| `EventRouter` (缓存回收) | 10min | ✅ |

### 端到端链路

| 链路 | 路径 | 状态 |
|------|------|:--:|
| 点击→激活→归因→回传 | ClickController→AttributionEngine→OaidMatcher→CallbackService→鲸鸿动能 | ✅ |
| 异步上报 | ReportController(async)→EventTask→EventWorker→AttributionEngine | ✅ |
| 再归因 | 激活锁冲突→checkReattribution→buildAttributionContext→enqueue | ✅ |
| 回传重试 | CallbackRetryService→分布式锁→指数退避→stale回收→dead | ✅ |
| 指纹降级 | FingerprintMatcher→IP前缀DB过滤→加权评分≥2 | ✅ |
| GAID/IDFA 降级 | OAID→GAID→IDFA→fingerprint 四级匹配链 | ✅ |

### 数据库迁移

| 版本 | 内容 | 状态 |
|------|------|:--:|
| V1 | 5 核心表 + 预置事件 | ✅ |
| V2 | callback_task + dedupe_key | ✅ |
| V3 | event_task + callback_rule + window_config + debug/reattribution | ✅ |
| V4 | gaid/idfa + version 乐观锁 | ✅ |
