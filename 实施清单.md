# 实施任务清单

---

## P0 - 核心归因（最小可用版本）

### 后端
- [ ] 创建 Spring Boot 项目骨架（attribution-server）
- [ ] 实现 `ClickController` — GET `/api/v1/click`，接收鲸鸿动能点击回调
- [ ] 实现 `ReportController` — POST `/api/v1/report`，接收客户端事件上报
- [ ] 实现 Redis 点击缓存服务 — key: `attribution:click:{game_id}:{oaid}`, TTL 7天
- [ ] 实现 `AttributionEngine` — OAID 匹配 + 归因窗口校验 + 去重
- [ ] 实现 `SignatureHelper` — HMAC-SHA256 签名
- [ ] 实现 `CallbackService` — 构造回传 JSON + POST 鲸鸿动能
- [ ] 实现 `CallbackRetryService` — 失败重试（最多3次，指数退避）
- [ ] 数据库建表 — game_config / click_record / attribution_record / callback_log
- [ ] 配置 HTTPS + NTP 时间同步

### 管理后台
- [ ] 游戏管理页面 — 新增/编辑游戏，配置 game_id + 密钥 + 归因窗口
- [ ] 归因查询页面 — 按条件查询归因记录

### 文档
- [ ] APK OAID 获取指引（已完成）
- [ ] 客户端接入指南（已完成）

### 联调验证
- [ ] 服务端自测（curl 模拟 click + report）
- [ ] 鲸鸿动能后台配置监测资产 + 联调
- [ ] 真实手机端到端测试

---

## P1 - 多平台完善

### 后端
- [ ] 实现事件配置系统 — 事件定义 CRUD + JSON Schema 校验
- [ ] 实现 `EventRouter` — 事件名 → conversion_type 映射
- [ ] 实现付费事件扩展字段支持（conversion_extend）

### 管理后台
- [ ] 事件配置页面 — 新增/编辑事件类型、配置参数、映射 conversion_type
- [ ] 回传日志页面 — 查看每次回传的请求/响应详情

### 文档
- [ ] HAP OAID 获取指引（已完成）
- [ ] RPK 归因适配指引（已完成）

### 联调验证
- [ ] HAP 端到端联调
- [ ] RPK 端到端联调

---

## P2 - 增强功能

### 后端
- [ ] 指纹降级匹配（FingerprintMatcher）— RPK OAID 失败时备选
- [ ] 多渠道支持预留（GAID/IDFA）
- [ ] 回传数据异步化（MQ 解耦）

### 管理后台
- [ ] 概览面板 — DAU/新增/付费/ROI 卡片
- [ ] 转化漏斗报表
- [ ] 数据导出 CSV

---

## P3 - 运营优化

### 后端
- [ ] 多语言支持（海外市场扩展）
- [ ] SDK 化封装（可选，降低接入门槛）

### 文档
- [ ] 接入文档自动生成（从事件配置导出）
