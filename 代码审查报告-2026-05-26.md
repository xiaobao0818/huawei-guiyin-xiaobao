# 华为鲸鸿动能通用自归因平台 — 全面代码审查报告

**审查日期**：2026-05-26  
**审查范围**：后端 (Spring Boot 3.2.5 + JPA + Redis) + 前端 (Vue 3 + Element Plus) + 部署配置  
**代码规模**：Java 后端 39 个源文件 + Vue 前端 6 个源文件 + 4 个 SQL 脚本  
**项目定位**：IAA 游戏自归因平台，对接华为鲸鸿动能广告，支持 APK/HAP/RPK 三种包体

---

## 一、严重问题（需立即修复）

### 1.1 🔴 开发环境加密密钥硬编码且长度不足

**文件**：`application-dev.yml` 第 26 行

```yaml
attribution:
  encryption-key: ${ENCRYPTION_KEY:dev-attribution-secret-key-32!!}
```

**问题**：
- 开发环境 fallback 值 `dev-attribution-secret-key-32!!` 是固定字符串，任何拿到源码的人都能解密 dev 环境数据
- 该值实际长度为 31 个字符（缺少 1 个字符才到 32），`AesUtil.fixKey()` 会补零到 32 字节，导致最后一个密钥字节为 0x00，削弱安全性
- 虽然 prod 配置强制从环境变量读取，但 dev 配置同样可能被误用

**建议**：开发环境也需要在 `.env` 中配置随机密钥，不要提供 fallback 默认值。如果必须有默认值用于 CI，至少使用 32 字符以上的随机字符串。

### 1.2 🔴 ClickController 完全无防护 — DoS 风险

**文件**：`ClickController.java` 第 18-121 行

`GET /api/v1/click` 端点存在的问题：
1. **无速率限制**：任何人可以无限量发送点击回调，快速填满 Redis 和 MySQL
2. **无来源校验**：虽然`callback`参数是必填的，但没有任何签名验证
3. **SecurityConfig 中 permitAll()**：该端点完全对外开放，仅在 `SecurityConfig.java` 中做了 `permitAll()` 配置
4. **OAID 为空时的行为不一致**：OAID 为空时跳过 Redis 缓存但依然写入 MySQL，攻击者可构造大量无 OAID 的请求绕开 Redis 限流

```java
// 第 85 行：OAID 为空时跳过 Redis 但照样写 MySQL！
if (!safeOaid.isEmpty()) {
    // ... Redis 写入
}
// 第 94 行：无论如何都写 MySQL
clickRecordRepo.save(record);
```

**建议**：
- 为 `/api/v1/click` 添加 IP 级别的速率限制（如 Guava RateLimiter 或 Spring Cloud Gateway）
- 在鲸鸿动能后台配置中添加预期的来源 IP 段验证
- OAID 为空时至少限制 Redis key 数目（如 "empty_oaid" + IP 组合）
- 考虑使用 Redis 滑动窗口实现 Token Bucket 限流

### 1.3 🔴 回传签名密钥可被日志泄露

**文件**：`CallbackService.java` 第 50-131 行

```java
// 第 57 行
String secretKey = AesUtil.decrypt(gameConfig.getSecretKey(), encryptionKey);
```

虽然在当前代码中没有直接日志打印 `secretKey`，但 `jsonBody` 在异常时打印入日志：
```java
// 第 95 行
log.warn("回传失败: ... body={}", response.getBody());
```

外部响应 body 不会被本服务控制，但如果鲸鸿动能返回的回传失败响应中包含了请求体回显，签名`Authorization`头可能泄露。更隐蔽的风险是：

**`SignatureUtil.buildAuthorizationHeader` 第 34 行**：
```java
return "Digest validTime=\"" + timestamp + "\", response=\"" + signature + "\"";
```

如果 `Authorization` 头的值被记录到任何日志系统或 APM 工具中，攻击者可以通过已知 body 反推密钥。

**建议**：日志中脱敏 `Authorization` 头，或者在构建 Authorization 头后不再将其写入任何可观测系统。

### 1.4 🔴 多 Pod 并发回调任务存在资源竞争

**文件**：`CallbackRetryService.java` 第 75-87 行

```java
for (CallbackTask task : dueTasks) {
    if (callbackTaskRepo.claimTask(task.getId(), DUE_STATUSES, LocalDateTime.now()) == 1) {
        processClaimedTask(task.getId());
    }
}
```

`claimTask` 使用 SQL UPDATE 实现乐观锁：
```sql
UPDATE callback_task SET status='sending', locked_at=:lockedAt
WHERE id=:id AND status IN :statuses
```

这个方案在单机运行是安全的。但当部署多个 Pod 时：
- `findTop50ByStatusInAndNextRetryAtLessThanEqual` 和 `claimTask` 之间没有事务原子性
- `@Scheduled(fixedDelayString)` 在每个 Pod 都独立运行，无法保证任务不分发给多个 Pod
- `locked_at` 超时回收机制使用同一个定时器，多 Pod 同时回收可能导致重复发送

当 Pod A 执行 `claimTask` 成功、Pod B 稍晚执行 `resetStaleSendingTasks` 时，Pod A 正在执行的任务可能被 Pod B 错误地重置为 `retry_pending`，导致同一个任务被 Pod C 重新认领，造成鲸鸿动能收到重复回传。

**建议**：
- 引入分布式锁（如 Redis `SET NX`）保护任务认领和回收
- 或在单 Pod 架构下运行回传 Worker（如用 `@ConditionalOnProperty` 控制）
- `locked_at` 回收逻辑应该检查 `locked_at < :cutoff AND status = 'sending'` 时确认没有其他 Pod 正在处理

---

## 二、高优先级问题

### 2.1 🟡 EventRouter ConcurrentHashMap 永不清理，内存泄漏风险

**文件**：`EventRouter.java` 第 19-20 行

```java
private final Map<String, EventDefinition> cache = new ConcurrentHashMap<>();
private final Map<String, EventDefinition> wildcardCache = new ConcurrentHashMap<>();
```

两个 Map 只有 `put` 操作，没有过期清理机制。虽然提供了 `clearCache` 和 `clearGame` 方法，但仅在管理后台手动操作事件时才触发。正常的事件查询流程会不断填充缓存，如果游戏数量多、事件名多，内存持续增长。

**建议**：使用 Guava Cache 或 Caffeine Cache，配置 TTL 和最大容量。至少添加定时清理（如每小时清空一次重建）。

### 2.2 🟡 ReportApiKeyFilter 密钥比对逻辑正确但路径匹配过于狭窄

**文件**：`ReportApiKeyFilter.java` 第 48-50 行

```java
private boolean requiresApiKey(HttpServletRequest request) {
    return HttpMethod.POST.matches(request.getMethod()) && "/api/v1/report".equals(request.getServletPath());
}
```

使用 `request.getServletPath()` 直接比较路径，这种方式是正确的且避免了路径遍历。但 `X-Attribution-Api-Key` Header 名称本身包含了具体业务含义，若未来需要扩展 Admin API 的另一种 key 校验，需要新建 Filter。

### 2.3 🟡 归因记录去重键生成逻辑存在边界情况

**文件**：`AttributionEngine.java` 第 253-273 行

```java
private String buildDedupeKey(ReportRequest request) {
    String oaid = getOaid(request);
    // ...
    if ("activate".equals(request.getEvent()) && oaid != null && !oaid.isEmpty()) {
        source = request.getGameId() + ":activate:" + oaid;
    } else if (request.getEventParams() != null) {
        // event_id > request_id > order_id (仅 purchase)
    }
    return source != null ? sha256Hex(source) : null;
}
```

当 source 为 null 时（非 activate 且无 event_id/request_id/order_id），`dedupeKey` 为 null，不会进行去重。这意味着 **同一用户在短时间内多次上报同一个非付费事件（如 `register` 不带 event_id）会产生多条归因记录**。

**建议**：
- 对没有自然幂等键的事件，使用 `gameId + event + oaid + truncateTimestamp` 组合生成去重键
- 或者对 event 字段设置一个时间窗口去重（如 5 分钟内同 game+event+oaid 只处理一次）

### 2.4 🟡 FingerprintMatcher 全表扫描风险

**文件**：`FingerprintMatcher.java` 第 37-43 行

```java
long since = System.currentTimeMillis() - (long) windowMinutes * 60 * 1000;
List<ClickRecord> candidates = clickRepo.findUnmatchedByGameAndTime(gameId, since);
for (ClickRecord click : candidates) {
    // 逐条计算分数...
}
```

`findUnmatchedByGameAndTime` 查询未匹配的点击记录。在高流量场景下，一个热门游戏在 30 分钟窗口内可能有数万条未匹配记录，全量加载到内存逐个计算 IP 前缀匹配会消耗大量内存和 CPU。

**建议**：
- 先用 IP 前缀在数据库层面做过滤（添加 `ip` 前缀查询条件）
- 限制候选集大小（如最多取 500 条）
- 或者将指纹匹配改由 Redis 的 Sorted Set 实现

### 2.5 🟡 DataCleanupTask 只清理点击和日志，不清理归因记录

**文件**：`DataCleanupTask.java` 第 27-33 行

```java
int deletedClicks = clickRepo.deleteByCreatedAtBefore(cutoff);
int deletedLogs = callbackLogRepo.deleteByCreatedAtBefore(cutoff);
```

只删除了 `click_record` 和 `callback_log`，但没有清理：
- `attribution_record`：归因记录持续累积，表会无限增长
- `callback_task`：已完成/失败的任务不会自动清理
- `event_definition`：预置事件不会被误删，但也没必要清理

**建议**：添加归因记录和回调任务的定期清理逻辑。或者至少添加分区表策略。

### 2.6 🟡 Dashboard 统计方法查询性能问题

**文件**：`AnalyticsService.java` 第 39-55 行

```java
dto.setTodayActivates(attributionRepo.countByEventAndSuccess("activate", todayStart, todayEnd));
dto.setTodayPurchases(attributionRepo.countByEventAndSuccess("purchase", todayStart, todayEnd));
Double revenue = attributionRepo.sumRevenueByDate(todayStart, todayEnd);
// ...
long totalCallbacks = attributionRepo.countCallbackAttemptsByDate(CALLBACK_ATTEMPT_STATUSES, todayStart, todayEnd);
long successCallbacks = attributionRepo.countCallbackAttemptsByDate(List.of("success"), todayStart, todayEnd);
```

每次看板加载会执行 **5 个独立的聚合查询**，且每 30 秒一次 Dashboard 打开就会产生这些查询。虽然每个查询本身有索引支持，但缺乏缓存。

**建议**：使用 Redis 缓存看板数据，每 1-5 分钟刷新一次，减少数据库压力。

---

## 三、中优先级问题

### 3.1 🟡 SQL 迁移脚本与 init.sql 重复维护

两个位置存在几乎相同的 DDL：
- `db/migration/V1__base_schema.sql` — Flyway 管理，生产环境使用
- `sql/init.sql` — 手工参考，但 README 说"仅保留为人工参考"

但两者存在差异：
- Flyway 版本没有 `dedupe_key` 列（在 V2 添加），而 `init.sql` 直接包含了 `dedupe_key`
- Flyway 版本没有 `callback_task` 表（在 V2 添加），而 `init.sql` 直接包含

这种重复可能导致手动执行 `init.sql` 创建的表结构与 Flyway 预期不一致。

**建议**：删除 `init.sql`，完全依赖 Flyway 管理。如果需要人工参考，增加 README 注释指向 Flyway 迁移文件。

### 3.2 🟡 application-dev.yml 和 application-prod.yml 配置差异中的隐患

**dev**：`ddl-auto: update`, `flyway: enabled: false`
**prod**：`ddl-auto: validate`, `flyway: enabled: true`

生产使用 Flyway 管理表结构（正确），但开发使用 Hibernate `ddl-auto: update`。这意味着开发环境的表结构由 Hibernate 实体映射决定，可能与 Flyway 迁移脚本产生偏差。例如：
- 如果开发者在实体上加了新字段但没有在 V3 迁移中添加，开发环境能正常运行但生产环境会部署失败
- JPA 的 `@Index` 注解（如 `AttributionRecord` 上的 `@Index`）在 H2 上可能不会被创建

**建议**：开发环境也启用 Flyway，或者至少增加 CI 步骤对比 JPA 实体与 Flyway 脚本的一致性。

### 3.3 🟡 前端 sessionStorage 存储明文凭证

**文件**：`attribution.ts` 第 19 行

```typescript
sessionStorage.setItem(AUTH_STORAGE_KEY, toBase64(`${username}:${password}`))
```

将 `username:password` 做 Base64 编码后存入 sessionStorage。虽然 sessionStorage 的生命周期在浏览器标签页关闭后结束，但在标签页存活期间，任何同源的 XSS 脚本都可以读取该值。这是 HTTP Basic Auth 的固有局限。

**建议**：短期无需改动（这是标准的 Basic Auth 前端实现），长期建议切换到 JWT Token 认证方式，token 有过期时间且不暴露原始密码。

### 3.4 🟡 前端分页索引不一致

**文件**：`AttributionData.vue` 第 112 行

```typescript
const res: any = await queryAttributions({ ...query, page: query.page - 1 })
```

前端 `page` 从 1 开始（`el-pagination` 的默认行为），但传给后端的 `page` 减了 1（`page - 1`）。而后端 `AnalyticsService` 中：
```java
int safePage = Math.max(page, 0); // 期望从 0 开始
```
前端和后端约定了 0-based 索引，但 `el-pagination` 使用 1-based。这是正确实现，但如果未来有人修改前端代码不知道这个约定，会导致分页错位。

**建议**：在前端 API 封装层（`attribution.ts`）统一处理分页偏移，而不是在视图中处理。

### 3.5 🟡 事件配置管理路径冲突风险

**EventConfigController.java** 第 13 行：`@RequestMapping("/admin/api")`  
**DashboardController.java** 第 19 行：`@RequestMapping("/admin/api")`

两个 Controller 共享同一个基路径。目前路由不冲突（`/games/{gameId}/events` vs `/attribution`, `/dashboard`），但：
- EventConfigController 只有 `/games/{gameId}/events` 和 `/events/**`
- DashboardController 有 `/dashboard`, `/attribution/**`, `/stats/**`, `/callback-logs/**`

如果未来有人给 EventConfigController 添加根路径路由（如 `@GetMapping("/events")`），可能与 DashboardController 冲突。

**建议**：合并没有特殊业务原因分开的 Controller，或者使用不同的路径前缀（如 `/admin/api/events` 和 `/admin/api/analytics`）。

### 3.6 🟡 PlatformType 枚举校验未被使用

**文件**：`PlatformType.java` 提供了 `fromCode()` 方法，但 `ReportRequest.platform` 字段只做了 `@NotBlank` 校验，没有 `@Pattern` 或其他验证来确保 platform 是 `apk/hap/rpk` 之一。

**建议**：添加自定义 Validator 注解或直接在 Controller/Engine 层面使用 `PlatformType.fromCode()` 校验。

### 3.7 🟡 编译产物管理缺失

`.gitignore` 缺少：
- `*.class` 文件（IDE 可能编译到 src 目录）
- `admin-frontend/dist/` 目录（虽然 `dist/` 在根 ignore 中，但 admin-frontend 路径下没有单独声明）

**建议**：在 `.gitignore` 中显式添加 `admin-frontend/dist/`。

---

## 四、低优先级问题 / 代码质量

### 4.1 后端

- **Entity 类使用手写 getter/setter**：项目添加了 Lombok 依赖但在所有 Entity 中手写了 getter 和 setter，代码冗余。建议使用 `@Data` 或 `@Getter @Setter`
- **AttributionEngine.ReportRequest 与 api.model.ReportRequest 完全重复**：两个内部类结构几乎相同，字段映射由 ReportController 手动进行。建议复用同一个类
- **ReportController 中手动属性复制**：第 30-49 行逐字段赋值，可用 MapStruct 或 BeanUtils 简化
- **CallbackService 中 RestTemplate 被注释标记为过时**：Spring 6.1+ 推荐使用 RestClient，RestTemplate 虽然仍可用但不是最佳实践
- **AnalyticsService 中硬编码事件名称**：`"activate"`, `"purchase"`, `"register"` 等字符串散落在代码中，建议提取为常量
- **GlobalExceptionHandler 中 BusinessException 的 HTTP 状态设为 OK**：`@ResponseStatus(HttpStatus.OK)` + `R.fail()` 的混合方式有些反直觉，客户端需要检查响应体中的 code 字段而非 HTTP 状态码
- **ClickController 同时接受 snake_case 和 camelCase 参数**：`game_id`/`gameId`, `campaign_id`/`campaignId` 等双份参数增加了参数组合的复杂性，鲸鸿动能宏参数统一使用 snake_case，camelCase 兼容可以去掉
- **HealthController 没有检查依赖服务**：健康检查只返回 "UP"，不检查 Redis/MySQL 连通性。不利于 Kubernetes 就绪探针
- **CallbackService.sendAttribution** 方法过长（131 行），执行了构造请求体、签名、发送、日志保存等多个职责，建议拆分
- **AttributionEngine.process 方法过长（179 行）**，包含校验、去重、匹配、记录构造、回调入队等所有逻辑
- **缺少单元测试和集成测试**：`pom.xml` 引用了 `spring-boot-starter-test` 但项目中没有任何测试类

### 4.2 前端

- **所有 API 响应使用 `any` 类型**：`const res: any = await getDashboard()`，丢失 TypeScript 类型检查的优势。建议定义完整的接口类型
- **Dashboard.vue 的快速操作按钮**：`@click="$router.push('/games')"` 直接在模板中导航，缺少类型安全
- **EventConfig.vue 删除预置事件无确认弹窗**：虽然按钮有 `:disabled="row.isPreset"`，但 API 层面 EventConfigService 也做了保护（抛异常），双重保护是好的
- **AttributionData.vue 的分页**：使用 `el-pagination` 但只绑定了 `current-change`，没有 `size-change` 事件处理
- **App.vue 的登录状态**：通过调用 `getDashboard()` 来验证凭证有效性，每次页面刷新都会产生一次 API 调用，可以缓存结果
- **vite.config.ts**：`server.proxy` 配置的 `/api` 和 `/admin` 会转发到开发环境后端，但生产部署时通过 Nginx 代理，两个环境行为不同但代码不需要修改，这是合理的

### 4.3 部署配置

- **docker-compose.yml 没有设置容器资源限制**：`redis` 的 `maxmemory 256mb` 是内存内的限制，但容器层面没有 `mem_limit`，MySQL 也没有限制
- **nginx.conf 缺少安全头**：没有 `X-Content-Type-Options`, `X-Frame-Options`, `Content-Security-Policy` 等
- **nginx.conf 的 proxy_read_timeout 只有 60s**：回传请求到鲸鸿动能可能超时（超时设置为 15s 在 CallbackService 中），但对于大文件上传可能不够
- **Docker Compose 的 admin-frontend 服务**：Dockerfile 中 `COPY admin-frontend/ ./` 在 build context 是项目根目录时正确，但 `COPY nginx.conf` 路径也依赖于 build context 为项目根目录

### 4.4 架构设计

- **归因引擎是同步的**：`ReportController` 中归因匹配和回传任务入队是同步执行的（虽然实际回传是异步的）。对于高 QPS 场景，建议将归因匹配也异步化（如通过消息队列解耦）
- **缺少 APM/可观测性**：虽然有 slf4j 日志，但没有集成 Micrometer/Prometheus 指标导出，无法监控归因匹配率、回传成功率等关键业务指标的趋势
- **Redis 点击缓存的 TTL 为 7 天**但 `AttributionEngine` 中激活锁的 TTL 为 180 天，两个 TTL 不一致可能造成混淆

---

## 五、项目亮点

在指出以上问题后，也要充分肯定项目中做得好的方面：

1. **整体架构设计清晰**：分层合理（api → core → common），职责明确
2. **归因匹配策略完整**：OAID 精准匹配 → 指纹降级 → 渠道号预留，覆盖了主要场景
3. **回传任务持久化设计优秀**：CallbackTask 表 + 指数退避 + 超时锁回收，服务重启不丢任务
4. **密钥安全存储**：AES-GCM 加密存储鲸鸿动能密钥，管理后台脱敏显示（`****`），密钥在内存中按需解密
5. **事件配置灵活**：game_id='*' 的预置事件 + 游戏级覆盖事件的设计，支持多游戏共享默认配置又允许个性化
6. **去重机制考虑了多维度**：activate 用 Redis 锁去重（180天），其他事件用业务幂等键（event_id/order_id）
7. **ReportApiKeyFilter 使用 MessageDigest.isEqual 做常量时间比较**：防止时序攻击，安全意识到位
8. **客户端接入协议简单**：纯 HTTP POST，引擎无关，接入门槛极低
9. **数据库索引设计合理**：关键查询都有复合索引（`idx_game_oaid`, `idx_callback_task_due` 等）
10. **Flyway 管理数据库版本**：生产环境使用 Flyway 而非 Hibernate ddl-auto，符合生产最佳实践

---

## 六、改进路线图建议

| 优先级 | 任务 | 预估工时 |
|--------|------|:---:|
| 🔴 立即 | 为 /api/v1/click 添加速率限制 | 2h |
| 🔴 立即 | 开发环境移除硬编码加密密钥 | 0.5h |
| 🔴 本周 | 多 Pod 回传任务并发竞争修复（引入分布式锁或单 Pod 模式） | 4h |
| 🔴 本周 | Dashboard 聚合查询添加缓存 | 2h |
| 🔴 本周 | DataCleanupTask 添加归因记录清理 | 1h |
| 🟡 两周 | EventRouter 缓存替换为 Caffeine（带 TTL） | 2h |
| 🟡 两周 | FingerprintMatcher 候选集大小限制和 DB 层筛选 | 3h |
| 🟡 两周 | 去重键生成逻辑改进（无幂等键事件的时间窗口去重） | 2h |
| 🟢 持续 | 添加单元测试和集成测试 | — |
| 🟢 持续 | 代码重构：拆分长方法、提取常量、统一类型定义 | — |
| 🟢 持续 | 添加 Prometheus 指标导出和 Grafana 看板 | — |

---

**总计新发现问题**：4 个严重、6 个高、7 个中、20+ 个低优先级问题

项目整体代码质量较好，架构设计合理，核心归因逻辑完整。主要风险集中在运维安全（速率限制、并发竞争、数据清理）方面，业务逻辑本身没有明显缺陷。
